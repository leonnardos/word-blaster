/// Versão nativa (APK/desktop): as páginas do site não existem — os links
/// do rodapé do menu nem aparecem.
class WebLinks {
  static const bool available = false;

  static void open(String path) {}
}
